const pauseButton = document.getElementById('pauseButton');
const stopButton = document.getElementById('stopButton');
const saveImgButton = document.getElementById('saveImgButton');
const windowedButton = document.getElementById('windowedButton');
const hideBarButton = document.getElementById('hideBarButton');

pauseButton.addEventListener('click', async () => {
    // ToDo
});

stopButton.addEventListener('click', async () => {
    // ToDo
});

saveImgButton.addEventListener('click', async () => {
    // ToDo
});

windowedButton.addEventListener('click', async () => {
    // ToDo
});

hideBarButton.addEventListener('click', async () => {
    // ToDo
});